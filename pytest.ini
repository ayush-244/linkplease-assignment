[pytest]
# Tell pytest-asyncio to automatically treat async test functions as coroutines.
# Without this, you'd need @pytest.mark.asyncio on every async test function.
asyncio_mode = auto
# Suppress the "loop scope unset" deprecation warning from pytest-asyncio.
asyncio_default_fixture_loop_scope = function
